//model.js
function enviarResultadoAlServidor(resultado) {
    // Aquí podrías implementar la lógica para enviar el resultado al servidor (por ejemplo, mediante una solicitud AJAX).
    console.log("El factorial es:", resultado);
    
}